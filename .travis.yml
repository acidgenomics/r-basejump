# R for travis
# https://docs.travis-ci.com/user/languages/r
language: r
sudo: false
cache: packages
dist: trusty
r:
  - bioc-release
  - bioc-devel
r_github_packages:
  - jimhester/lintr
  - r-lib/covr
after_success:
  - Rscript -e 'lintr::lint_package()'
  - Rscript -e 'covr::codecov()'
